# 🧠 Python Glossary for Melisa

| Term         | Definition                                                                 | Example                                      |
|--------------|----------------------------------------------------------------------------|----------------------------------------------|
| `variable`   | A name to store data                                                       | `name = "Melisa"`                            |
| `string`     | Text surrounded by quotes                                                  | `"Hello"` or `'Hi'`                          |
| `int`        | A whole number                                                             | `age = 13`                                   |
| `float`      | A number with a decimal                                                    | `pi = 3.14`                                  |
| `list`       | A collection of items in order                                             | `colors = ["blue", "pink", "green"]`         |
| `function`   | A reusable block of code                                                   | `def greet(): print("Hi!")`                  |
| `if`         | Makes a decision based on condition                                        | `if mood == "happy": print("Yay!")`          |
| `for`        | Loops through a list or range                                              | `for item in list:`                          |
| `while`      | Loops while a condition is true                                            | `while count < 5:`                           |
| `input()`    | Gets input from the user                                                   | `name = input("What’s your name? ")`         |
| `print()`    | Shows a message on the screen                                              | `print("Hello!")`                            |
| `import`     | Loads a module or tool                                                     | `import random`                              |
| `random`     | Built-in module for random numbers                                         | `random.choice(["yes", "no"])`               |
| `dictionary` | A set of key-value pairs                                                   | `{"name": "Melisa", "age": 13}`              |
